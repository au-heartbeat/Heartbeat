query getPipelineInfo($slug: ID, $first: Int) {
  organization(slug: $slug) {
    pipelines(first: $first) {
      edges {
        node {
          slug
          name
          repository {
            url
          }
          steps {
            yaml
          }
        }
      }
    }
  }
}
